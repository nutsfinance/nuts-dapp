import { TokenValuePipe } from './token-value.pipe';

describe('TokenValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TokenValuePipe();
    expect(pipe).toBeTruthy();
  });
});
